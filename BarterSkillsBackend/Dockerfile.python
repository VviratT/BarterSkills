FROM python:3.11-slim
WORKDIR /app

COPY BarterSkillsBackend/qg_service.py .

RUN pip install --no-cache-dir torch transformers fastapi uvicorn openai-whisper

CMD ["uvicorn", "qg_service:app", "--host", "0.0.0.0", "--port", "8001"]
